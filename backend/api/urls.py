from django.urls import path, include

# This file only includes the API URLs that are imported into the main urls.py
# The actual endpoint configurations are in backend/saep/urls.py
urlpatterns = [
    # This file is kept for compatibility but doesn't define any endpoints directly
]